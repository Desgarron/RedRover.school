def adder(x: int, y: int) -> float:
    return x + y


def subtracts(x: int, y: int) -> float:
    return x - y


def multy(x: int, y: int) -> float:
    return x*y


def divider(x: int, y: int) -> float:
    return x / y
